import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PMDM02-TareaEvaluativa01',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
